# Dependencies
node_modules/

# macOS
.DS_Store

# Local env files
.env
.env.local

# Build scratch and generated artifacts — never commit these.
work/
dist/
# Batch driver outcome log (scripts/batch-build.ts)
failures.log
# Planetiler cache (downloaded base layers + temp)
data/
tools/planetiler.jar
*.osm.pbf
